[project]
name = "neopixel-pi5"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "viam-sdk>=0.55.1",
    "pi5neo; sys_platform == 'linux'",
    "pydantic>=2.11.9",
]
